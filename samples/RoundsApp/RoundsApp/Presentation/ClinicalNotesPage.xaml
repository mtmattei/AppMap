<Page x:Class="RoundsApp.Presentation.ClinicalNotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <utu:AutoLayout Orientation="Vertical"
                  Spacing="12"
                  Padding="24"
                  PrimaryAxisAlignment="Center"
                  CounterAxisAlignment="Center">
    <TextBlock Text="ClinicalNotes" Style="{StaticResource TitleLarge}" />
      <Button Content="Back to vitals" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="VitalsEntry" />
      <Button Content="Back" Style="{StaticResource TextButtonStyle}" uen:Navigation.Request="-" />
  </utu:AutoLayout>
</Page>
