<Page x:Class="RoundsApp.Presentation.AppShellPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <utu:AutoLayout Orientation="Vertical"
                  Spacing="12"
                  Padding="24"
                  PrimaryAxisAlignment="Center"
                  CounterAxisAlignment="Center">
    <TextBlock Text="AppShell" Style="{StaticResource TitleLarge}" />
      <Button Content="Open dashboard" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="Dashboard" />
      <Button Content="Settings" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="Settings" />
  </utu:AutoLayout>
</Page>
