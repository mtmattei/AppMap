<Page x:Class="RoundsApp.Presentation.PatientDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
  <utu:AutoLayout Orientation="Vertical"
                  Spacing="12"
                  Padding="24"
                  PrimaryAxisAlignment="Center"
                  CounterAxisAlignment="Center">
    <TextBlock Text="PatientDetail" Style="{StaticResource TitleLarge}" />
      <Button Content="Start rounding" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="RoundingChecklist" />
      <Button Content="Record vitals" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="VitalsEntry" />
      <Button Content="Administer meds" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="MedAdministration" />
      <Button Content="Add note" Style="{StaticResource FilledButtonStyle}" uen:Navigation.Request="ClinicalNotes" />
      <Button Content="Back" Style="{StaticResource TextButtonStyle}" uen:Navigation.Request="-" />
  </utu:AutoLayout>
</Page>
